# ============================================================================
# Supabase Configuration
# ============================================================================

# Supabase Project URL (from your Supabase project settings)
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Supabase Anonymous Key (safe to use in client-side code)
# This key is used for client-side authentication and RLS-protected queries
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (NEVER expose to client-side!)
# Only use this in server-side code (API routes, Server Components)
# This key bypasses Row Level Security - use with caution
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# Legacy Database (if still using Vercel Postgres)
# ============================================================================

# POSTGRES_URL=postgres://username:password@localhost:5432/vietnamese_words_db
# POSTGRES_URL_NON_POOLING=postgres://username:password@localhost:5432/vietnamese_words_db

# ============================================================================
# NextAuth.js (optional if using Supabase Auth)
# ============================================================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# For production, also add:
# NEXTAUTH_URL=https://your-domain.com